<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ciia.webeirinterface.model.db.Archivo">
	<select id="getCatArchivo" resultType="Archivo">
		SELECT
			idArchivo,
			ruta,			
			nombre,			
			activo
		FROM ARCHIVO
	</select>
	<select id="getListaArchivo" resultType="Archivo">
		SELECT
			idArchivo,
			ruta,			
			nombre,			
			activo
		FROM ARCHIVO
		WHERE activo = TRUE
	</select>
	<select id="getArchivo" parameterType="Archivo" resultType="Archivo">
		SELECT
			idArchivo,
			ruta,			
			nombre,			
			activo
		FROM ARCHIVO
		WHERE idArchivo = #{idArchivo}
	</select>
	<update id="updateArchivo" parameterType="Archivo">
		UPDATE ARCHIVO
		SET ruta = #{ruta},			
			nombre = #{nombre},			
			activo = #{activo}
		WHERE idArchivo = #{idArchivo}
	</update>
	<insert id="insertArchivo" parameterType="Archivo" keyProperty="idArchivo">
		INSERT INTO ARCHIVO
			(ruta,			
			nombre,			
			activo)
		VALUES
			(#{ruta},			
			#{nombre},			
			#{activo})
	</insert>
</mapper>