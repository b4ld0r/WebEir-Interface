<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ciia.webeirinterface.model.db.Bitacora">
	<select id="getListaBitacora" resultType="Bitacora">
		SELECT
			idBitacora,
			fecha,			
			hora,			
			campo,			
			idAccion,		
			idModuloSistema,		
			idUsuario		
		FROM BITACORA
	</select>
	<select id="getBitacora" parameterType="Bitacora" resultType="Bitacora">
		SELECT
			idBitacora,
			fecha,			
			hora,			
			campo,			
			idAccion,		
			idModuloSistema,		
			idUsuario		
		FROM BITACORA
		WHERE idBitacora = #{idBitacora}
	</select>
	<update id="updateBitacora" parameterType="Bitacora">
		UPDATE BITACORA
		SET	fecha = #{fecha},			
			hora = #{hora},			
			campo = #{campo},			
			idAccion = #{idAccion},		
			idModuloSistema = #{idModuloSistema},		
			idUsuario = #{idUsuario}
		WHERE idBitacora = #{idBitacora}
	</update>
	<insert id="insertBitacora" parameterType="Bitacora" keyProperty="idBitacora">
		INSERT INTO BITACORA
			(fecha,			
			hora,			
			campo,			
			idAccion,		
			idModuloSistema,		
			idUsuario)
		VALUES
			(#{fecha},			
			#{hora},			
			#{campo},			
			#{idAccion},		
			#{idModuloSistema},		
			#{idUsuario})
	</insert>
</mapper>