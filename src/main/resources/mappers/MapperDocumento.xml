<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ciia.webeirinterface.model.db.Documento">
	<select id="getCatDocumento" resultType="Documento">
		SELECT 
			idDocumento,
			descripcion,			
			obligatorio,			
			activo			
		FROM DOCUMENTO
	</select>
	<select id="getListaDocumento" resultType="Documento">
		SELECT 
			idDocumento,
			descripcion,			
			obligatorio,			
			activo			
		FROM DOCUMENTO
		WHERE activo = TRUE
	</select>
	<select id="getDocumento" parameterType="Documento" resultType="Documento">
		SELECT 
			idDocumento,
			descripcion,			
			obligatorio,			
			activo			
		FROM DOCUMENTO
		WHERE idDocumento = #{idDocumento}
	</select>
	<update id="updateDocumento" parameterType="Documento">
		UPDATE DOCUMENTO
		SET descripcion = #{descripcion},			
			obligatorio = #{obligatorio}
		WHERE idDocumento = #{idDocumento}
	</update>
	<insert id="insertDocumento" parameterType="Documento" keyProperty="idDocumento">
		INSERT INTO DOCUMENTO
			(descripcion,			
			obligatorio,			
			activo)
		VALUES
			(#{descripcion},			
			#{obligatorio},			
			#{activo})
	</insert>
	<update id="borradoLogico" parameterType="Documento">
		UPDATE DOCUMENTO
		SET activo = #{activo}
		WHERE idDocumento = #{idDocumento}
	</update>
</mapper>